var score = 100;
var score_result = score >= 60 ? "Pass!" : "Fail!";
// 判斷 Pass 或 fail

if (score === 100) {
    console.log("You are No.1!");
    // 如果是 100 分就顯示 You are No.1!
}
else
    console.log(score_result);